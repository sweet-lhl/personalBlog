栈内存：作用域
  1.提供一个供js代码自上而下执行的环境（代码都是在栈中执行的）
  2.由于基本数据类型值比较简单，他们都是直接在栈内存中开辟一个位置，把值直接存进去的
  => 当栈内存被销毁，存储的那些基本值也都跟着销毁了
  
  
堆内存：引用值对应的空间
  1.存储引用类型值的（对象：键值对  函数：代码字符串）
  =>当前堆内存释放销毁，那么这个引用值彻底没了
  =>堆内存的释放：当堆内存没有被任何的变量或者其他东西所占用，浏览器会在空闲的时候，自主地进行内存回收
  把所有不被占用的堆内存销毁掉（谷歌浏览器）